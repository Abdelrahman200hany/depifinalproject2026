import 'package:flutter/material.dart';

abstract class AppColor {
    static const kPrimaryColordLight = Color.fromARGB(255, 34, 93, 202);

  static const kPrimaryColor = Color(0xff2D6CDF);
  static const kPrimaryColordark = Color.fromARGB(255, 34, 93, 202);
  static const ksecondaryColor = Color(0xffCA9744);
    static const kRatingColor = Color(0xffB60F1A);
    static const kGreyAppColor = Color(0xFF949D9E);

    static const Color primaryBlue = Color(
        0xFF2563EB); // الأزرق الرئيسي للزراير
    static const Color accentBlue = Color(
        0xFFEFF6FF); // الأزرق الفاتح جداً (خلفية الحالات)
    static const Color textMain = Color(0xFF191C1E); // لون النصوص الأساسي
    static const Color textSub = Color(0xFF64748B); // لون النصوص الفرعية
    static const Color bgGrey = Color(0xFFF8FAFC); // لون خلفية الشاشة
    static const Color success = Color(0xFF10B981); // أخضر
    static const Color warning = Color(0xFFF59E0B); // برتقالي
    static const Color danger = Color(0xFFEF4444); // أحمر

}
